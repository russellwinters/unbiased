# Database
DATABASE_URL="postgresql://unbiased:unbiased_dev_password@localhost:5433/unbiased"

# Postgres Docker Configuration (for docker-compose)
POSTGRES_USER="unbiased"
POSTGRES_PASSWORD="unbiased_dev_password"
POSTGRES_DB="unbiased"

# OpenAI (for embeddings and bias analysis)
OPENAI_API_KEY="sk-..."

# Redis (for caching)
UPSTASH_REDIS_REST_URL="https://..."
UPSTASH_REDIS_REST_TOKEN="..."

# App Configuration
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NODE_ENV="development"
